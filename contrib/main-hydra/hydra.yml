serve:
  cookies:
    same_site_mode: Lax

urls:
  self:
    issuer: http://localhost:8846
  consent: about:invalid/consent
  login: about:invalid/login
  logout: about:invalid/logout

secrets:
  system:
    - youReallyNeedToChangeThis

oidc:
  subject_identifiers:
    supported_types:
      - pairwise
      - public
    pairwise:
      salt: youReallyNeedToChangeThis

log:
  leak_sensitive_values: true
